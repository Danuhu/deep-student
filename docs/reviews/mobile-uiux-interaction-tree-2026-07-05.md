# Deep Student 移动 UI 交互树审查（2026-07-05）

> 方法：`ui-audit-mobile-tree.mjs` 在 400×880 窗口逐页打开抽屉、遍历子路径并截图。
> 截图：`/tmp/ds-mobile-audit/tree-shots/`

环境：window 10223 · bridge connected

## 核心结论（P0）

**无 P0**。移动抽屉已融合为单滚动容器（页内区 + 「应用」全局导航），见 `MobileSlidingLayout` + `MobileSidebarNavigation embedded`。 

## 交互树覆盖矩阵

| 视图 | 抽屉 | 双栏堆叠 | 全局导航 | 子路径 |
|------|------|----------|----------|--------|
| 新会话 | 有 | 否 | 底部 8 项 | 4 |
| 学习资源 | 有 | 否 | 底部 8 项 | 1 |
| 待办 | 有 | 否 | 底部 8 项 | 1 |
| 技能管理 | 有 | 否 | 底部 8 项 | 1 |
| 制卡任务 | 有 | 否 | 底部 8 项 | 2 |
| 模板管理 | 有 | 否 | 底部 8 项 | 2 |

## P2 问题清单

- **NAV-OFFSCREEN** · 学习资源：底部导航未完全可见：模板管理、设置（统一滚动抽屉，可滚到底）

## 各页交互路径（应测尽测）

### 新会话 (chat-v2)
- 顶栏 ☰ → 抽屉：新对话 / 课题 / 最近 / 未分组 / 已归档 + 底部全局 8 项
- 冗余：页内「新对话」≈ 底部「新会话」
- 抽屉开时顶栏 `hidden`（sessionSheetOpen），符合全屏侧栏但加剧「两段式」观感
- 子路径：新对话、已归档、搜索与命令；右滑附件/资源库（未在本轮自动跑）

### 学习资源
- 抽屉：文件夹树 + 底部全局导航
- 空态「右键新建」→ P1
- 三屏：文件预览右 panel（LearningHub 特有，需手势/点击文件）

### 待办
- 抽屉：收件箱/清单/标签 + 底部全局导航
- 主区番茄钟药丸 + 任务列表

### 技能管理
- 抽屉：页内标题区 + 底部全局导航（与其它页一致）
- 右滑进入编辑器；筛选 Tab + 创建/导入

### 制卡任务 / 模板管理
- 抽屉：页内工具 + 底部全局导航（模板页与用户截图一致）
- 设置 → 移动全屏 Settings Sheet

## 修复方向（供排期）

1. **融合侧栏**：单滚动容器 + section 标题（「本页」/「应用」），去掉 border-t 硬切；或长页内区时折叠全局 nav
2. **去重**：当前页从底部 nav 隐藏或降权；统一「新对话/新会话」文案
3. **技能页 IA**：补汉堡抽屉或底部入口，与 chat/todo 一致
4. **设置**：移动全屏 Settings 视图，或 Sheet 全屏遮罩 + 独立标题
5. **自动化**：抽屉开时顶栏 hidden，审计脚本须先 `resetDrawers()`