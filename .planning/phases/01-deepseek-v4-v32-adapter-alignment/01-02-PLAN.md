# Plan 01-02: Harden DeepSeek Provider Request Dialects

**Phase:** 1 - DeepSeek V4/V3.2 Adapter Alignment
**Status:** Complete
**Requirements:** DSK-01, DSK-02, DSK-03, DSK-04, DSK-05, DSK-06, DSK-07, DSK-08, DSK-12, DSK-13

## Goal

Ensure DeepSeek request construction uses official V4 thinking parameters for official DeepSeek, preserves SiliconFlow V3.2 behavior, and future-proofs SiliconFlow V4-shaped models without creating a separate adapter.

## Tasks

1. Inspect `src-tauri/src/llm_manager/adapters/deepseek.rs` and related adapter routing in `src-tauri/src/llm_manager/adapters/mod.rs`.
2. Confirm official V4 requests emit `thinking.type=enabled` plus `reasoning_effort=high|max`, or `thinking.type=disabled` when thinking is off.
3. Ensure UI/internal disabled thinking never becomes a user-facing `none` budget option.
4. Strip or suppress `temperature`, `top_p`, `presence_penalty`, and `frequency_penalty` for official V4 thinking payloads.
5. Preserve SiliconFlow V3.2 request behavior and sampling controls.
6. Add a future SiliconFlow V4 path that applies V4 capability semantics while preserving SiliconFlow provider field naming.
7. Ensure official API requests never inject Hugging Face max-reasoning prompt text.

## Must Haves

- One `DeepSeekAdapter` remains the family integration point.
- Official V4 high/max/disabled payloads are covered by tests.
- SiliconFlow V3.2 compatibility is covered by tests.
- Future SiliconFlow V4-shaped model behavior is covered by tests.

## Verification

- Run focused Rust adapter tests.
- Run the settings reasoning-effort Vitest contract if UI-facing behavior changes.
- Inspect serialized request payloads in tests for absence/presence of expected fields.
